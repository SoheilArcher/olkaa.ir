# یک کلید تصادفی بگذارید (مثلاً خروجی دستور زیر):
# python -c "import secrets; print(secrets.token_urlsafe(50))"
SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=*

# --- برای استفاده از PostgreSQL این‌ها را پر کنید (در غیر این صورت SQLite استفاده می‌شود) ---
# POSTGRES_DB=fava
# POSTGRES_USER=fava
# POSTGRES_PASSWORD=secret
# POSTGRES_HOST=127.0.0.1
# POSTGRES_PORT=5432
