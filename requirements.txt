# --- پایه (برای اجرای اولیه کافی است) ---
Django>=5.0,<6.0
python-dotenv>=1.0
gunicorn>=26.0
django-unfold>=0.70

# --- فاز بعد: هرکدام را هنگام نیاز از حالت توضیح خارج کنید ---
# psycopg2-binary>=2.9      # PostgreSQL (نیازمند libpq-dev روی اوبونتو)
# celery>=5.3               # کارهای پس‌زمینه (ایمیل، مؤدیان، صورتحساب دوره‌ای)
# redis>=5.0                # بروکر برای Celery
# django-jalali>=7.0        # تاریخ شمسی
# weasyprint>=60            # تولید PDF فاکتور فارسی
# django-netfields>=1.3     # فیلدهای inet/cidr برای اجاره‌ی IP (IPAM)
